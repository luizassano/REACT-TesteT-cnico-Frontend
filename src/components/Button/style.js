import styled from "styled-components";

export const ButtonStyled = styled.button`
  width: 80%;
  padding: 5px;
  background-color: var(--clr-title-blue);
  border: 1px solid var(--clr-input-outline);
  border-radius: 3px;
  color: var(--clr-input-border);
`;